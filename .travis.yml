language: python
python:
  - '2.7'
  - '3.6'
install:
  - pip install pytest
script:
  - pytest
before_deploy:
  - tar cvzf package_$TRAVIS_BUILD_NUMBER ./test_things.py
deploy:
  providers: releases
  api_key: $API_KEY
  file: simple
  skip_cleanup: true
  on:
    tags: true